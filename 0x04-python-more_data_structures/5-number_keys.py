#!/usr/bin/python3
def number_keys(a_dictionary):
    """ number_keys - number of keys in a dict

        Args:
            a_dictionary: dict to loop

        Returns:
            length of keys
    """
    return len(a_dictionary.keys())
